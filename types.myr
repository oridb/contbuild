use std

pkg contbuild =
	type target = struct
		tip	: byte[:]
		targ	: byte[:]
		repo	: byte[:]
		scratch	: byte[:]
		build	: byte[:][:]
		clean	: byte[:][:]
		test	: byte[:][:]
		fix	: byte[:][:]

		/* computed dirs */
		_repo	: byte[:]
	;;

;;
